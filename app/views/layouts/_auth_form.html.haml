-# Auth wrapper — modernized glassmorphism card
.min-h-screen.h-full.relative.flex.items-center.justify-center.bg-gradient-to-br.from-slate-900.via-slate-800.to-slate-900
  .absolute.inset-0.z-0.opacity-40
    = image_tag "bg.webp", alt: "", class: "w-full h-full object-cover mix-blend-overlay blur-sm"
  .relative.z-10.w-full
    .mx-auto.max-w-md
      .p-8.rounded-2xl.shadow-2xl.backdrop-blur-xl{ class: "bg-white/10 border border-white/20" }
        .flex.flex-col.items-center
          = image_tag "siberdiri.webp", alt: "Logo TKJ PERIODIK", class: "mb-4 w-16 h-16 rounded-xl shadow-md ring-2 ring-red-300/60 bg-white"
          %h1.text-2xl.font-extrabold.tracking-tight.text-white.text-center
            = yield :page_title
          %p.mt-1.mb-6.text-sm.text-slate-200.text-center
            TKJ PERIODIK
        .space-y-5
          = yield
        .mt-6.flex.flex-col.items-center.justify-between.gap-2
          - if @forgot_page || @otp_page
            = link_to "Masuk", new_user_session_path, class: "inline-block font-semibold text-sm text-red-300 hover:text-white transition-colors"
            = link_to "Daftar", new_user_registration_path, class: "inline-block font-semibold text-sm text-red-300 hover:text-white transition-colors"
          - else
            = render "users/shared/links"