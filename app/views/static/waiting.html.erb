<% content_for :title do %>
  <% @title %>
<% end %>
<div class="flex items-center justify-center h-screen bg-gray-100">
  <div class="p-8 bg-white rounded-lg shadow-xl max-w-[480px]">
    <% if current_user.pending? %>
    <h1 class="text-2xl font-semibold text-center">Mohon Tunggu Verifikasi</h1>
    <p class="my-4 text-center">Akun Anda saat ini sedang dalam proses verifikasi <% if current_user.is_forgotten? %>ulang <span class="font-bold uppercase">karena lupa password</span><% end %> oleh operator, yang dapat memakan waktu hingga 3 hari kerja. Mohon periksa status akun Anda secara berkala.</p>
    <p class="mb-4 text-center">Jika verifikasi belum selesai dalam jangka waktu tersebut, harap segera menghubungi operator untuk informasi lebih lanjut.</p>
    <% else %>
    <h1 class="text-2xl font-semibold text-center">
      <%= @title %>
    </h1>
    <p class="my-4">Alasan: <%= current_user.account_status_reason %></p>
    <p class="mb-4">Silahkan menghubungi operator jika terjadi kesalahan.</p>
    <% end %>
    <div class="flex justify-center">
      <%= link_to(destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline btn-error flex items-center justify-center space-x-2") do %>
        <span class="inline-block">
          <svg xmlns="http://www.w3.org/2000/svg" class="w-5 h-5" fill="none" viewbox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"></path>
          </svg>
        </span>
        <span>Keluar</span>
      <% end %>
    </div>
  </div>
</div>
